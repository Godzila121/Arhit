@using FoodDelivery.Models
@model IEnumerable<MenuItem>

@{
    ViewData["Title"] = "Меню ресторану";
    var restaurantName = Model.FirstOrDefault()?.Restaurant.Name ?? "Наш";
    // Вказуємо, що потрібно використовувати головний шаблон сайту
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container my-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="display-5 fw-bold">Меню "@restaurantName"</h1>
            <p class="text-muted">Оберіть страви, які вам до смаку.</p>
        </div>
        <a asp-controller="Restaurant" asp-action="Index" class="btn btn-outline-secondary">
             Повернутись до ресторанів
        </a>
    </div>

    <div class="row row-cols-1 row-cols-md-2 g-4">
        @if (Model.Any())
        {
            @foreach (var item in Model)
            {
                <div class="col">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body d-flex flex-column">
                            <div class="d-flex justify-content-between">
                                <h5 class="card-title">@item.Name</h5>
                                <h5 class="card-title text-primary">@item.Price.ToString("C")</h5>
                            </div>
                            <p class="card-text flex-grow-1">@(item.Description ?? "Опис страви відсутній.")</p>
                            <a asp-area="" asp-controller="Cart" asp-action="AddToCart" asp-route-menuItemId="@item.Id" class="btn btn-primary align-self-start">
                                🛒 Додати в кошик
                            </a>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12">
                <div class="alert alert-info" role="alert">
                    На жаль, у цього ресторану ще немає страв у меню.
                </div>
            </div>
        }
    </div>
</div>